import { Crown, Timer } from "lucide-react";
import { Button } from "@/components/ui/button";
import { royalTheme } from "./royalTheme";
export function RoyalEarlyAccess() { return <section className={`relative overflow-hidden rounded-[2rem] border p-6 ${royalTheme.hero}`} aria-labelledby="royal-early-access-heading"><div className="relative flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><p className={`flex items-center gap-2 text-xs font-semibold uppercase tracking-[0.2em] ${royalTheme.accent}`}><Crown className="h-4 w-4" /> VIP early access</p><h2 id="royal-early-access-heading" className="mt-2 text-2xl font-semibold text-[#F8E7B0]">Exclusive before everyone else.</h2><p className="mt-1 text-sm text-stone-300">Early access for Royal customers.</p></div><div className="flex items-center gap-3"><span className={`rounded-full border px-3 py-2 text-xs ${royalTheme.chip}`}><Timer className="mr-1 inline h-4 w-4" /> Countdown soon</span><Button type="button" className={royalTheme.primaryButton}>Unlock</Button></div></div></section>; }
