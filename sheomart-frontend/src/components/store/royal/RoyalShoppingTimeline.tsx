import { Check, Crown, PackageCheck, Rocket, Sparkles, Truck } from "lucide-react";
import { royalTheme } from "./royalTheme";
const steps = [{ icon: Check, label: "Order Confirmed" }, { icon: PackageCheck, label: "Luxury Packaging" }, { icon: Rocket, label: "Priority Dispatch" }, { icon: Truck, label: "Concierge Tracking" }, { icon: Crown, label: "Delivered" }, { icon: Sparkles, label: "Luxury Finish" }];
export function RoyalShoppingTimeline() { return <section className={`rounded-[2rem] border p-5 sm:p-7 ${royalTheme.panel}`} aria-labelledby="royal-timeline-heading"><h2 id="royal-timeline-heading" className={`text-2xl font-semibold ${royalTheme.panelText}`}>Royal Shopping Timeline</h2><div className="mt-6 grid gap-4 sm:grid-cols-3 lg:grid-cols-6">{steps.map(({ icon: Icon, label }, index) => <div key={label} className="relative flex items-center gap-3 sm:block sm:text-center"><div className={`mx-0 flex h-10 w-10 items-center justify-center rounded-full border sm:mx-auto ${royalTheme.badge}`}><Icon className="h-4 w-4" /></div><p className="text-xs font-semibold text-stone-300 sm:mt-3">{label}</p>{index < steps.length - 1 ? <span className="hidden h-px bg-[#D4AF37]/40 lg:absolute lg:left-1/2 lg:top-5 lg:block lg:w-full" aria-hidden="true" /> : null}</div>)}</div></section>; }
