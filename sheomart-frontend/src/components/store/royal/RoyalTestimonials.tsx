import { Quote, Star } from "lucide-react";
import { royalTheme } from "./royalTheme";

const testimonials = [{ name: "Aarav M.", quote: "A beautifully considered shopping experience." }, { name: "Mira S.", quote: "The attention to detail makes every order feel special." }, { name: "Kabir R.", quote: "Exceptional quality and wonderfully reliable service." }];

export function RoyalTestimonials() { return <section className="space-y-4" aria-labelledby="royal-testimonials-heading"><div className="flex items-center gap-2"><Quote className={`h-5 w-5 ${royalTheme.icon}`} /><h2 id="royal-testimonials-heading" className={`text-2xl font-semibold ${royalTheme.panelText}`}>Trusted by Premium Buyers</h2></div><div className="flex gap-4 overflow-x-auto pb-2 [scrollbar-width:none] [&::-webkit-scrollbar]:hidden">{testimonials.map(({ name, quote }) => <article key={name} className={`min-w-[270px] rounded-3xl border p-5 ${royalTheme.panel} ${royalTheme.hover}`}><div className="flex gap-1" aria-label="5 out of 5 stars">{Array.from({ length: 5 }, (_, index) => <Star key={index} className={`h-4 w-4 fill-current ${royalTheme.icon}`} />)}</div><p className="mt-4 text-sm leading-6 text-stone-300">“{quote}”</p><div className="mt-4 flex items-center gap-3"><div className={`flex h-9 w-9 items-center justify-center rounded-full border ${royalTheme.badge}`}>{name.charAt(0)}</div><span className={`text-xs font-semibold ${royalTheme.panelText}`}>{name}</span></div></article>)}</div></section>; }
