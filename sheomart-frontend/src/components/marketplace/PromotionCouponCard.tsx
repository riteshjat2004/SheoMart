import type { CouponItem } from "@/services/promotions";

const money = (value: number) => `₹${value.toLocaleString("en-IN")}`;

export function PromotionCouponCard({ coupon, copied, onCopy }: { coupon: CouponItem; copied: boolean; onCopy: (code: string) => void }) {
  return <article className="rounded-[1.75rem] border border-dashed border-emerald-300 bg-emerald-50/70 p-5 dark:border-emerald-900/70 dark:bg-emerald-950/20"><div className="flex items-start justify-between gap-3"><div><h3 className="font-semibold text-stone-900 dark:text-stone-50">{coupon.title}</h3><p className="mt-2 text-lg font-semibold text-emerald-700 dark:text-emerald-300">{coupon.discountType === "percentage" ? `${coupon.discountValue}% off` : `${money(coupon.discountValue)} off`}</p></div><span className="rounded-full bg-white/80 px-2.5 py-1 text-xs font-medium text-emerald-700 dark:bg-stone-900/80 dark:text-emerald-300">Min {money(coupon.minimumCartValue)}</span></div><div className="mt-4 flex items-center justify-between gap-3 rounded-lg border border-emerald-200 bg-white/80 px-3 py-2 dark:border-emerald-900/70 dark:bg-stone-950/60"><code className="font-semibold tracking-wider text-emerald-800 dark:text-emerald-200">{coupon.code}</code><button type="button" onClick={() => onCopy(coupon.code)} className="inline-flex items-center gap-1 text-xs font-medium text-emerald-700 dark:text-emerald-300" aria-label={`Copy coupon ${coupon.code}`}>{copied ? "Copied" : "Copy code"}</button></div><p className="mt-3 text-xs text-stone-500 dark:text-stone-400">Expires {new Date(coupon.endsAt).toLocaleDateString("en-IN", { day: "numeric", month: "short" })}</p></article>;
}
