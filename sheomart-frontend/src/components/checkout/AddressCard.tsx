import { Check, Pencil, Trash2 } from "lucide-react";
import type { AddressItem } from "@/services/addresses";

export function AddressCard({ address, selected, onSelect, onEdit, onDelete }: { address: AddressItem; selected: boolean; onSelect: () => void; onEdit: () => void; onDelete: () => void }) {
  return <article className={`rounded-xl border p-4 transition motion-reduce:transition-none ${selected ? "border-emerald-500 bg-emerald-50/70 shadow-[0_0_0_3px_rgba(16,185,129,0.12)] dark:bg-emerald-500/10" : "border-stone-200 dark:border-stone-800"}`}><button type="button" onClick={onSelect} className="w-full text-left"><div className="flex items-center justify-between gap-3"><span className="rounded-full bg-stone-100 px-2.5 py-1 text-xs font-semibold capitalize text-stone-600 dark:bg-stone-800 dark:text-stone-300">{address.addressType ?? "other"}</span>{selected ? <Check className="h-5 w-5 text-emerald-600" /> : null}</div><p className="mt-3 font-semibold">{address.fullName}</p><p className="mt-1 text-sm text-stone-600 dark:text-stone-300">{address.mobile}</p><p className="mt-2 text-sm text-stone-600 dark:text-stone-300">{[address.house, address.street, address.landmark, address.city, address.state, address.pincode].filter(Boolean).join(", ")}</p>{address.isDefault ? <span className="mt-3 inline-block rounded-full bg-emerald-100 px-2.5 py-1 text-xs font-semibold text-emerald-700">Default</span> : null}</button><div className="mt-3 flex gap-2 border-t border-stone-200 pt-3 dark:border-stone-800"><button type="button" onClick={onEdit} className="inline-flex items-center gap-1 text-xs font-semibold text-stone-600 hover:text-emerald-600"><Pencil className="h-3.5 w-3.5" />Edit</button><button type="button" onClick={onDelete} className="inline-flex items-center gap-1 text-xs font-semibold text-stone-600 hover:text-rose-600"><Trash2 className="h-3.5 w-3.5" />Delete</button></div></article>;
}
