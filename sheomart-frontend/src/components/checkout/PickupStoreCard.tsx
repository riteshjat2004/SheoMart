import { MapPin } from "lucide-react";

export function PickupStoreCard({ store }: { store: { address?: string; city?: string; state?: string; pincode?: string } | null }) { const address = [store?.address, store?.city, store?.state, store?.pincode].filter(Boolean).join(", "); return <div className="rounded-xl border border-stone-200 bg-stone-50 p-4 dark:border-stone-800 dark:bg-stone-950/60"><p className="flex items-center gap-2 font-semibold"><MapPin className="h-4 w-4 text-emerald-600" />Pickup location</p><p className="mt-2 text-sm text-stone-600 dark:text-stone-300">{address || "Store address unavailable"}</p><a className="mt-3 inline-block text-sm font-semibold text-emerald-700" href={`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(address)}`} target="_blank" rel="noreferrer">Open in Google Maps</a><p className="mt-2 text-xs text-stone-500">Bring your order ID when collecting your order.</p></div>; }
