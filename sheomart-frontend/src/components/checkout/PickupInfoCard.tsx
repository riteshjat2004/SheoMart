import { Clock3, MapPin, Phone } from "lucide-react";

export function PickupInfoCard({ store }: { store: { storeName?: string; name?: string; address?: string; city?: string; state?: string; pincode?: string; phone?: string } | null }) {
  const address = [store?.address, store?.city, store?.state, store?.pincode].filter(Boolean).join(", ");
  const details = [["Store Name", store?.storeName ?? store?.name ?? "Store unavailable", ""], ["Pickup Address", address || "Address unavailable", "map"], ["Store Timings", "10:00 AM - 8:00 PM", ""], ["Store Phone", store?.phone ?? "Phone unavailable", "phone"], ["Estimated Pickup Time", "30 - 45 minutes", "clock"], ["Pickup Instructions", "Bring your order ID when collecting your order.", ""]] as const;
  return <section className="rounded-2xl border border-stone-200 bg-white p-6 shadow-sm dark:border-stone-800 dark:bg-stone-900"><div className="flex items-start justify-between gap-4"><div><h2 className="text-lg font-semibold text-stone-900 dark:text-stone-50">Pickup Information</h2><p className="mt-1 text-sm text-stone-600 dark:text-stone-300">Details for your selected pickup location.</p></div><MapPin className="h-5 w-5 text-emerald-600" /></div><div className="mt-5 grid gap-3 sm:grid-cols-2">{details.map(([label, value, icon]) => <div key={label} className="rounded-xl bg-stone-50 p-4 dark:bg-stone-950/60"><div className="flex items-center justify-between gap-2"><p className="text-xs font-semibold uppercase tracking-[0.12em] text-stone-500">{label}</p>{label === "Store Timings" ? <span className="rounded-full bg-emerald-100 px-2 py-1 text-[10px] font-bold text-emerald-700 dark:bg-emerald-500/15 dark:text-emerald-300">OPEN HOURS</span> : null}</div><p className="mt-2 flex items-center gap-2 text-sm font-medium text-stone-800 dark:text-stone-200">{icon === "map" ? <MapPin className="h-4 w-4 text-emerald-600" /> : icon === "phone" ? <Phone className="h-4 w-4 text-emerald-600" /> : icon === "clock" ? <Clock3 className="h-4 w-4 text-emerald-600" /> : null}{value}</p></div>)}</div></section>;
}